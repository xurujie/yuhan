<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yuhan.dao.IssueDao">

    <select id="findByType" resultType="Issue">
        select id,title,answer,type from issue where type = #{param1};
    </select>

</mapper>